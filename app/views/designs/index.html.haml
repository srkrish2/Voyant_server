%a.btn-primary.btn(href="#{new_design_path}") New Design

%hr

.container
  .row
    .span12
      %ul.thumbnails
        - @designs.each do |design|
          %li.span2(style="background: #eef")
            %a.thumbnail(href="#{edit_design_path(design)}")
              %image(src="#{design.picture.url(:thumb)}")
            %h3= design.name
            %p= design.description
            %button.btn.btn-success Publish
            = link_to 'Delete', design_path(design), :method => :delete, :class => "btn btn-danger", :confirm => 'Confirm to Delete?'

